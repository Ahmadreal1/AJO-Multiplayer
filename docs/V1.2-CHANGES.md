# AJO v1.2 Changes

Real PostgreSQL deployment readiness.

## Implemented
- `scripts/check-readiness.js` – reports OK / MISSING / NOT CONNECTED
- Production database guidance (managed Postgres + SSL)
- Version 1.2.0
- Explicit non-claim: no live managed instance is connected in this environment

## Unchanged
- All AJO voting rules
- JSON default adapter
- PostgreSQL adapter, schema, migrate, and verify scripts from v1.1

## Live verification status
**NOT CONNECTED** — `DATABASE_URL` is not configured in this environment.
When a real database is available, run:

    npm run db:migrate && npm run db:verify

Only a successful `db:verify` constitutes live PostgreSQL verification.
