AJO v0.6 is the architecture milestone.
It defines the production database schema, API contract, Android build plan and release checklist.
The current runnable prototype remains in the project root and still uses the development JSON persistence layer.
No external database or cloud service has been falsely marked as connected.
